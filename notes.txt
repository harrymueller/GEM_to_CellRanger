GEM -> barcodes.tsv, genes.tsv, matrix.mtx, spatial.tsv

########################
barcodes.tsv psuedo
########################
CREATE 2x arrays x & y
FOR EACH line IN GEM:
    CHECK IF x_start and y_start in arrays:
        ADD IF NOT
SORT arrays
FOR EACH PAIR:
    CREATE barcode
        OUTPUT

########################
genes.tsv
########################
CREATE char* array;
FOR EACH l in GEM:
    IF NEW GENE: ADD to array
CHECK SORTED
OUTPUT



